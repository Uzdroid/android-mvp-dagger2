<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.how.simplemvp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MvpApplication"
        >

        <activity android:name=".view.activity.LoginActivity" android:noHistory="true" android:label="@string/login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.ProfileActivity" android:label="@string/my_github"/>

    </application>
</manifest>
